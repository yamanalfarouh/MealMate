<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidProject"
        tools:targetApi="31">

        <!-- Set MainActivity as the only launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- All other activities are non-launcher activities -->
        <activity
            android:name=".home"
            android:exported="true" />

        <activity
            android:name=".meals"
            android:exported="true" />

        <activity
            android:name=".itemsmanage"
            android:exported="true" />

        <activity
            android:name=".favgroc"
            android:exported="true" />

        <activity
            android:name=".view_groc"
            android:exported="true" />

        <activity
            android:name=".ThirdActivity"
            android:exported="true" />

        <activity
            android:name=".create_recipe"
            android:exported="true" />

        <activity
            android:name=".FirstActivity"
            android:exported="true" />

        <activity
            android:name=".SecondActivity"
            android:exported="true" />

        <activity
            android:name=".register"
            android:exported="true" />

        <activity
            android:name=".login"
            android:exported="true" />

    </application>
</manifest>
